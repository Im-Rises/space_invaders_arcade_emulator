pub struct Cpu {
    a: u8,
    f: u8,
    b: u8,
    c: u8,
    d: u8,
    e: u8,
    h: u8,
    l: u8,
    sp: u16,
    pc: u16,
    stat: u16,
}

impl Cpu {
    fn read_opcode(&self)
    {
        // self.a = 5;
    }

    fn compute_opcode()
    {
        //Implement a big switch case here.
    }

    // List all the opcodes function here
}